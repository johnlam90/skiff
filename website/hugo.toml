baseURL = "https://johnlam90.github.io/skiff/"
title = "Skiff"
locale = "en-us"
defaultContentLanguage = "en"
enableGitInfo = true
enableRobotsTXT = true
disableKinds = ["taxonomy", "term"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 4
  [markup.highlight]
    noClasses = false
    style = "tokyonight-night"
    lineNos = false
    tabWidth = 2

[params]
  description = "A mouse-first terminal code editor for SSH workflows. One static Go binary, no setup."
  github_url = "https://github.com/johnlam90/skiff"
  brew_tap = "johnlam90/skiff"
  install_script_url = "https://raw.githubusercontent.com/johnlam90/skiff/main/install.sh"
  releases_url = "https://github.com/johnlam90/skiff/releases"
  # The Pages workflow rewrites this from internal/version/version.go on
  # every deploy, so it stays in lockstep with the published binary.
  # Bump this manually if you want a fresh local build to display the
  # current version without running CI.
  version = "v0.0.25"
  ogImage = "/og-image.png"

[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]

[outputFormats]
  [outputFormats.RSS]
    mediatype = "application/rss+xml"
    baseName = "feed"

[sitemap]
  changefreq = "weekly"
  priority = 0.5
  filename = "sitemap.xml"

[menu]
  [[menu.main]]
    name = "Docs"
    url = "/docs/"
    weight = 10
  [[menu.main]]
    name = "Install"
    url = "/#install"
    weight = 20
  [[menu.main]]
    name = "GitHub"
    url = "https://github.com/johnlam90/skiff"
    weight = 30
    [menu.main.params]
      external = true

[security]
  [security.funcs]
    getenv = ["^HUGO_", "^CI$"]
